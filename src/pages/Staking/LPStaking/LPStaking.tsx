import { Bottom, Container, Top } from './styled-components';

const LPStaking: React.FC = () => {
    return (
        <Container>
            <Top></Top>
            <Bottom></Bottom>
        </Container>
    );
};

export default LPStaking;
